---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "contentful_locale Resource - terraform-provider-contentful"
subcategory: ""
description: |-
  Manages a Contentful Locale in an environment. The locale code identifies translated content; Contentful generates a separate system ID. Destroying a locale permanently deletes its associated localized content. Contentful rejects deletion while another locale uses it as a fallback. Import existing locales before managing them. If creation returns an ambiguous error, inspect Contentful and import any created locale before trying again.
---

# contentful_locale (Resource)

Manages a Contentful Locale in an environment. The locale code identifies translated content; Contentful generates a separate system ID. Destroying a locale permanently deletes its associated localized content. Contentful rejects deletion while another locale uses it as a fallback. Import existing locales before managing them. If creation returns an ambiguous error, inspect Contentful and import any created locale before trying again.

## Example Usage

```terraform
resource "contentful_locale" "example" {
  space_id       = var.contentful_space_id
  environment_id = var.contentful_environment_id

  name          = "German"
  code          = "de-DE"
  fallback_code = "en-US"
  optional      = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `code` (String) Locale code, for example `en-US`. Changing it updates the locale in place and affects subsequent content API responses. Contentful rejects a code change while another locale uses it as a fallback.
- `environment_id` (String) ID of the environment containing the locale. Use a concrete environment ID for a stable management target. Changing this value replaces the resource.
- `name` (String) Name of the locale.
- `space_id` (String) ID of the space containing the locale. Changing this value replaces the resource.

### Optional

- `content_delivery_api` (Boolean) Whether the locale is available through the Content Delivery API. Defaults to true.
- `content_management_api` (Boolean) Whether the locale is available through the Content Management API. Defaults to true.
- `fallback_code` (String) Locale code to use when translated content is missing. Omission sends null to clear the fallback; an empty string is sent unchanged for Contentful to validate.
- `optional` (Boolean) Whether the locale can be empty for required localized fields. Defaults to false.
- `timeouts` (Attributes) (see [below for nested schema](#nestedatt--timeouts))

### Read-Only

- `default` (Boolean) Whether this is the environment's default locale. This property cannot be changed through this resource.
- `id` (String) Composite Terraform resource identifier in `space_id/environment_id/locale_id` form.
- `locale_id` (String) System ID generated by Contentful, distinct from the locale code.

<a id="nestedatt--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Choose one of the following methods. Match the resource address and Contentful IDs to your configuration, then review the plan before applying.

### Import by identity

In Terraform v1.12.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import) with `identity`:

```terraform
import {
  identity = {
    space_id       = var.contentful_space_id
    environment_id = var.contentful_environment_id
    locale_id      = var.contentful_locale_id
  }
  to = contentful_locale.example
}
```

### Import by ID

In Terraform v1.5.0 and later, use an [`import` block](https://developer.hashicorp.com/terraform/language/import) with `id`:

```terraform
import {
  id = "${var.contentful_space_id}/${var.contentful_environment_id}/${var.contentful_locale_id}"
  to = contentful_locale.example
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `environment_id` (String)
- `locale_id` (String)
- `space_id` (String)
