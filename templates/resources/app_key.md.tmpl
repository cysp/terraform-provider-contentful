---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
  {{ .Description | plainmarkdown | trimspace }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExamples -}}
## Example Usage

{{ tffile .ExampleFile }}
{{- end }}

## Private Key Handling

When `jwk` is omitted or set to `null`, Contentful generates a key pair and returns the private key once in the create response. The provider stores that value in the sensitive `private_key` attribute and preserves it during later refreshes because Contentful does not return it again.

When `jwk` is configured, it contains public key material only. The practitioner is responsible for managing the corresponding private key outside this provider, and `private_key` is `null`.

Imported app keys expose the public `jwk`, but `private_key` cannot be recovered, even if the remote key was originally generated by Contentful.

`private_key` is marked sensitive, so Terraform redacts it in normal CLI output, but it is still stored in Terraform state. Use a state backend and access controls appropriate for private key material.

{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport -}}
## Import

Import is supported using the following syntax:

{{ if .HasImportIdentityConfig -}}
In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

{{ tffile .ImportIdentityConfigFile }}

{{ .IdentitySchemaMarkdown | trimspace }}

{{- end }}

{{ if .HasImportIDConfig -}}
In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

{{ tffile .ImportIDConfigFile }}
{{- end }}

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{ codefile "shell" .ImportFile }}
{{- end }}
