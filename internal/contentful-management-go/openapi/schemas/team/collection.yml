TeamCollection:
  type: object
  properties:
    sys:
      type: object
      properties:
        type:
          type: string
          enum:
            - Array
      required:
        - type
    total:
      type: integer
    skip:
      type: integer
    limit:
      type: integer
    items:
      type: array
      items:
        $ref: "./list-item.yml#/TeamListItem"
  required:
    - sys
    - items
