AppSigningSecretSys:
  allOf:
    - $ref: "../sys/sys.yml"
    - $ref: "../sys/with-organization.yml"
    - $ref: "../sys/with-app-definition.yml"
    - type: object
      properties:
        type:
          type: string
          enum:
            - AppSigningSecret
