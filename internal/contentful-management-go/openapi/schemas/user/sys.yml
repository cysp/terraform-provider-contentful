UserSys:
  allOf:
    - $ref: "../sys/sys.yml"
    - $ref: "../sys/with-id.yml"
    - $ref: "../sys/with-version.yml"
    - type: object
      properties:
        type:
          type: string
          enum:
            - User
