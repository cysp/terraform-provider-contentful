LinkSys:
  allOf:
    - $ref: "../sys/sys.yml"
    - $ref: "../sys/sys-with-id.yml"
    - type: object
      properties:
        type:
          type: string
          enum:
            - Link
        linkType:
          type: string
      required:
        - linkType
