SpaceSys:
  allOf:
    - $ref: "../sys/with-type.yml"
    - $ref: "../sys/with-id.yml"
    - $ref: "../sys/with-organization.yml"
    - type: object
      properties:
        type:
          enum:
            - Space
