AppKeyCollection:
  type: object
  properties:
    sys:
      type: object
      properties:
        type:
          type: string
          enum:
            - Array
      required:
        - type
    total:
      type: integer
    items:
      type: array
      items:
        $ref: "./app-key.yml#/AppKey"
  required:
    - sys
    - total
    - items
