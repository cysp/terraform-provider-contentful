AppDefinitionSys:
  allOf:
    - $ref: "../sys/sys-with-organization.yml"
    - $ref: "../sys/sys-with-id.yml"
    - type: object
      properties:
        type:
          type: string
          enum:
            - AppDefinition
