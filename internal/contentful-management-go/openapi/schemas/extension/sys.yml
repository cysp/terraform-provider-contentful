ExtensionSys:
  allOf:
    - $ref: "../sys/sys-with-space-environment.yml"
    - $ref: "../sys/sys-with-id-and-version.yml"
    - type: object
      properties:
        type:
          type: string
          enum:
            - Extension
